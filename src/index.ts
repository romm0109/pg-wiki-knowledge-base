export { createClient, Client } from './client';
export type {
  ClientConfig,
  MetadataFilters,
  WithTenant,
  IngestResult,
  DeleteResult,
  QueryResult,
  PageSummary,
  PageDetail,
} from './types';
export type { LLMAdapter, EmbeddingAdapter } from './llm';
